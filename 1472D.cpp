#include "bits/stdc++.h"
using namespace std;

int main() {
	int t;
	cin >> t;
	while (t--) {
		int n;
		cin >> n;
		vector<int> arr(n);
		while (n--) {
			int x;
			cin >> x;
			arr.push_back(x);		}
	} 
}